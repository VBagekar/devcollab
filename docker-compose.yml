version: '3.8'

services:
  mongo:
    image: mongo:7.0
    container_name: devcollab-mongo
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7.2-alpine
    container_name: devcollab-redis
    ports:
      - '6379:6379'

volumes:
  mongo-data: